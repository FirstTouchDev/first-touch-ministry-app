import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'FTM App',
  webDir: 'dist/first-touch-ministry-app/browser',
};

export default config;
